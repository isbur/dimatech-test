[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: tests that need Postgres; use ephemeral dimatech_test DB (skipped if unavailable)
